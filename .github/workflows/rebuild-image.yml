# .github/workflows/rebuild-image.yml
# This workflow is triggered by the remediation agent when a container
# vulnerability needs a rebuild. It requires manual approval before building.

name: Rebuild Container Image

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag name for the rebuild'
        required: true
      cve_id:
        description: 'CVE being remediated'
        required: true

jobs:
  approve:
    name: Approve Rebuild
    runs-on: ubuntu-latest
    environment: production  # <-- requires environment approval in GitHub settings
    steps:
      - name: Approval gate
        run: |
          echo "✅ Rebuild approved for tag: ${{ inputs.tag }}"
          echo "   CVE: ${{ inputs.cve_id }}"

  build:
    name: Build & Push Image
    needs: approve
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.REGISTRY }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ vars.REGISTRY }}/${{ vars.IMAGE_NAME }}:${{ inputs.tag }}
            ${{ vars.REGISTRY }}/${{ vars.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            CVE_REMEDIATION=${{ inputs.cve_id }}

      - name: Verify image
        run: |
          echo "🐳 Image built and pushed with tag: ${{ inputs.tag }}"
          echo "   Remediation for: ${{ inputs.cve_id }}"
